% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_db.R
\name{tabulergm_default_plotfun}
\alias{tabulergm_default_plotfun}
\title{Default Plot Function for Term Figures}
\usage{
tabulergm_default_plotfun(netobj, layout, vcolor, ecolor, directed, ...)
}
\arguments{
\item{netobj}{A \link[network:network]{network::network} object.}

\item{layout}{A two-column numeric matrix of node coordinates (x, y).}

\item{vcolor}{Character vector of vertex colors.}

\item{ecolor}{Character vector of edge colors.}

\item{directed}{Logical. Whether the network is directed.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Called for its side-effect of drawing a plot on the current
graphics device. Returns invisibly.
}
\description{
Draws a network figure using \code{\link[netplot:nplot]{netplot::nplot()}}. This is the default
plot function used by \code{tabulergm} when generating term figures.
}
\details{
A plot function must accept the arguments \code{netobj}, \code{layout}, \code{vcolor},
\code{ecolor}, \code{directed}, and \code{...}, and draw on the current graphics device.
}
\examples{
\dontrun{
# See the default implementation
tabulergm_default_plotfun

# Use a custom plot function
my_plotfun <- function(netobj, layout, vcolor, ecolor, directed, ...) {
  netplot::nplot(netobj, vertex.color = vcolor, edge.color = ecolor,
                 layout = layout)
}
tabulergm_set_plotfun(my_plotfun)
}
}
\seealso{
\code{\link[=tabulergm_set_plotfun]{tabulergm_set_plotfun()}}, \code{\link[=tabulergm_get_plotfun]{tabulergm_get_plotfun()}}
}
