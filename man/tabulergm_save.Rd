% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulergm_save.R
\name{tabulergm_save}
\alias{tabulergm_save}
\alias{tabulergm_save.ergm}
\alias{tabulergm_save.formula}
\alias{tabulergm_save.data.frame}
\title{Save a tabulergm Table and Figure Assets}
\usage{
tabulergm_save(object, path, ...)

\method{tabulergm_save}{ergm}(
  object,
  path,
  format = c("markdown", "latex"),
  filename = "tabulergm-table",
  images_dir = "figures",
  overwrite = TRUE,
  latex_image_width = "0.7in",
  ...
)

\method{tabulergm_save}{formula}(
  object,
  path,
  format = c("markdown", "latex"),
  filename = "tabulergm-table",
  images_dir = "figures",
  overwrite = TRUE,
  latex_image_width = "0.7in",
  ...
)

\method{tabulergm_save}{data.frame}(
  object,
  path,
  format = c("markdown", "latex"),
  filename = "tabulergm-table",
  images_dir = "figures",
  overwrite = TRUE,
  latex_image_width = "0.7in",
  ...
)
}
\arguments{
\item{object}{A fitted \link[ergm:ergm]{ergm} object, an ERGM
\link[stats:formula]{formula}, or a \code{data.frame} returned by
\code{\link[=tabulergm_table]{tabulergm_table()}}.}

\item{path}{Target directory. It is created recursively when needed.}

\item{...}{For \code{ergm} and \code{formula} methods, additional arguments passed to
\code{\link[=tabulergm_table]{tabulergm_table()}}. For the \code{data.frame} method, additional arguments are
ignored.}

\item{format}{Character vector of output formats to write. Supported values
are \code{"markdown"} and \code{"latex"}. By default both are written.}

\item{filename}{File name stem for the table code files, without extension.
Defaults to \code{"tabulergm-table"}.}

\item{images_dir}{Directory, relative to \code{path}, where copied images should
be stored. Use \code{"."} to place images directly in \code{path}.}

\item{overwrite}{Logical. Overwrite existing table code and image files?
Default \code{TRUE}.}

\item{latex_image_width}{Character width passed to \verb{\\\\includegraphics} in
LaTeX output. Default \code{"0.7in"}.}
}
\value{
Invisibly returns a list with:
\describe{
\item{path}{The normalized export directory.}
\item{files}{Named character vector of written table code files.}
\item{figures}{Named character vector of copied figure files, where names
are the relative paths used in the table.}
\item{table}{The data frame used for export, with \code{figure} paths rewritten
to the copied relative paths.}
}
}
\description{
Saves the table code produced by \code{tabulergm} and copies the generated term
figures out of the temporary cache into a user-controlled directory. The
exported table references the copied image files with relative paths, making
the result easier to reuse in another document or project.
}
\section{Methods (by class)}{
\itemize{
\item \code{tabulergm_save(ergm)}: Method for fitted ergm objects.

\item \code{tabulergm_save(formula)}: Method for formula objects.

\item \code{tabulergm_save(data.frame)}: Method for data frames returned by
\code{tabulergm_table()}.

}}
\examples{
if (requireNamespace("knitr", quietly = TRUE)) {
  fit <- readRDS(system.file("fits", "fit_edges.rds", package = "tabulergm"))
  out_dir <- tempfile("tabulergm-export-")
  tabulergm_save(fit, out_dir, include_math = TRUE)
}
}
\seealso{
\code{\link[=tabulergm_table]{tabulergm_table()}}
}
