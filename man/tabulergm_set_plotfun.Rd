% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_db.R
\name{tabulergm_set_plotfun}
\alias{tabulergm_set_plotfun}
\title{Set the Plot Function for Term Figures}
\usage{
tabulergm_set_plotfun(plotfun)
}
\arguments{
\item{plotfun}{A function with signature
\verb{function(netobj, layout, vcolor, ecolor, directed, ...)}.}
}
\value{
Invisibly returns the previous plot function.
}
\description{
Replaces the current plot function used when drawing term figures.
The function must accept \code{netobj}, \code{layout}, \code{vcolor}, \code{ecolor},
\code{directed}, and \code{...}. Setting a plot function invalidates previously
cached term figures, so subsequent renders use the new function.
}
\examples{
my_plotfun <- function(netobj, layout, vcolor, ecolor, directed, ...) {
  netplot::nplot(netobj, vertex.color = vcolor, edge.color = ecolor,
                 layout = layout)
}
old <- tabulergm_get_plotfun()
tabulergm_set_plotfun(my_plotfun)
# Restore the previous plot function
tabulergm_set_plotfun(old)
}
\seealso{
\code{\link[=tabulergm_default_plotfun]{tabulergm_default_plotfun()}}, \code{\link[=tabulergm_get_plotfun]{tabulergm_get_plotfun()}}
}
