% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulergm_table.R
\name{tabulergm_table}
\alias{tabulergm_table}
\alias{tabulergm_table.ergm}
\alias{tabulergm_table.formula}
\title{Generate a Publication-Ready Table from an ERGM Object}
\usage{
tabulergm_table(object, ...)

\method{tabulergm_table}{ergm}(
  object,
  include_description = FALSE,
  include_math = FALSE,
  include_attribute = FALSE,
  format = c("data.frame", "html", "markdown"),
  ...
)

\method{tabulergm_table}{formula}(object, format = c("data.frame", "html", "markdown"), ...)
}
\arguments{
\item{object}{A fitted \link[ergm:ergm]{ergm} object or an ERGM
\link[stats:formula]{formula}.}

\item{...}{Additional arguments passed to methods.}

\item{include_description}{Logical. Include the term description column?
Default \code{FALSE}.}

\item{include_math}{Logical. Include the mathematical notation column?
Default \code{FALSE}.}

\item{include_attribute}{Logical. Include the attribute column? Default
\code{FALSE}.}

\item{format}{Character. Output format: \code{"data.frame"} (default),
\code{"html"}, or \code{"markdown"}. HTML and Markdown output require the
\pkg{knitr} package.}
}
\value{
A \code{data.frame} (default), or a \code{knitr_kable} object when
\code{format} is \code{"html"} or \code{"markdown"}.
}
\description{
S3 generic that dispatches to methods for fitted \link[ergm:ergm]{ergm} objects
or plain \link[stats:formula]{formula} objects, calling the internal parsing
engine and returning a formatted table.
}
\section{Methods (by class)}{
\itemize{
\item \code{tabulergm_table(ergm)}: Method for fitted ergm objects.

Calls \code{\link[=parse_ergm_model]{parse_ergm_model()}} and returns a table with default columns
\code{term}, \code{figure}, \code{estimate}, \code{se}, and \code{pvalue}.
Optional columns (\code{description}, \code{math}, \code{attribute}) can be included
via logical arguments.

\item \code{tabulergm_table(formula)}: Method for formula objects.

Calls \code{\link[=parse_ergm_formula]{parse_ergm_formula()}} and returns a table with columns
\code{term}, \code{figure}, \code{math}, and \code{description}. Coefficient statistics
are excluded because no fitted model is available.

}}
\examples{
\dontrun{
library(ergm)
data(florentine)
fit <- ergm(flomarriage ~ edges + nodematch("wealth"))
tabulergm_table(fit)
tabulergm_table(fit, include_description = TRUE)
tabulergm_table(fit, format = "markdown")
}
\dontrun{
library(ergm)
tabulergm_table(network ~ edges + nodematch("gender"))
}
}
\seealso{
\code{\link[=tabulergm_table.ergm]{tabulergm_table.ergm()}}, \code{\link[=tabulergm_table.formula]{tabulergm_table.formula()}}
}
